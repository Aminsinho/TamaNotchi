import SwiftUI

/// Escenas SwiftUI reservadas para ajustes o futuras ventanas estándar.
/// El proceso arranca con `TamaNotchiBootstrap` (ver `TamaNotchiBootstrap.swift`).
struct TamaNotchiApp: App {
    var body: some Scene {
        Settings {
            EmptyView()
        }
    }
}

